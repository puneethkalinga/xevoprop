/**
 * XEVOPROP — PROPERTIES DATA
 * Hardcoded mock properties have been removed.
 * All property listings are now loaded dynamically from the live database.
 */

export const properties = [];
export const FALLBACK_PROPERTIES = properties;
export default properties;